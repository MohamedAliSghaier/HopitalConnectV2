<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Ordonnance Médicale</title>
    <style>
        body {
            font-family: "Times New Roman", serif;
            font-size: 13px;
            color: #000;
            margin: 40px;
        }

        .top-bar {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .top-bar .cabinet-info {
            font-size: 14px;
            line-height: 1.4;
        }

        .top-bar .date {
            text-align: right;
            font-size: 13px;
        }

        .title {
            text-align: center;
            font-size: 20px;
            margin-bottom: 30px;
            font-weight: bold;
            text-decoration: underline;
        }

        .doctor-patient {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
        }

        .doctor-info, .patient-info {
            width: 48%;
        }

        .section {
            margin-bottom: 20px;
        }

        .section h2 {
            font-size: 14px;
            margin-bottom: 8px;
            border-bottom: 1px solid #000;
            padding-bottom: 3px;
        }

        table.prescription-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        table.prescription-table th,
        table.prescription-table td {
            border: 1px solid #000;
            padding: 6px;
            text-align: left;
            font-size: 13px;
        }

        .signature-section {
            margin-top: 60px;
            display: flex;
            justify-content: space-between;
        }

        .signature-box {
            width: 40%;
            height: 100px;
            border: 1px solid #000;
            padding: 10px;
            position: relative;
        }

        .signature-title {
            position: absolute;
            bottom: 10px;
            left: 10px;
            font-weight: bold;
        }

        .footer {
            margin-top: 40px;
            font-size: 12px;
            text-align: center;
            border-top: 1px dashed #000;
            padding-top: 10px;
        }
    </style>
</head>
<body>

    <div class="top-bar">
        <div class="cabinet-info">
            <strong>Cabinet Médical Dr. {{ ordonnance.medecin.id.nom }}</strong><br>
            12 Rue des Médecins, Tunis<br>
            Tél : (+216) 71 123 456<br>
            Email : contact@cabinet.tn
        </div>
        <div class="date">
            Tunis, le <strong>{{ ordonnance.datePrescription|date('d/m/Y') }}</strong>
        </div>
    </div>

    <div class="title">
        ORDONNANCE MÉDICALE
    </div>

    <div class="doctor-patient">
        <div class="doctor-info">
            <h2>Médecin</h2>
            <p>
                <strong>Dr. {{ ordonnance.medecin.id.nom }}</strong><br>
                Spécialité : {{ ordonnance.medecin.specialite }}
            </p>
        </div>

        <div class="patient-info">
            <h2>Patient</h2>
            <p>
                <strong>{{ ordonnance.patient.nom }}</strong>
            </p>
        </div>
    </div>

    <div class="section">
        <h2>Prescription</h2>
        <table class="prescription-table">
            <thead>
                <tr>
                    <th>Médicament</th>
                    <th>Quantité</th>
                </tr>
            </thead>
            <tbody>
                {% for medicament in ordonnance.medicaments %}
                    {% set parts = medicament|split(':') %}
                    <tr>
                        <td>{{ parts[0] }}</td>
                        <td>{{ parts[1] }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="section">
        <h2>Instructions</h2>
        <p>{{ ordonnance.instructions }}</p>
    </div>

    <div class="signature-section">
        <div class="signature-box">
            <span class="signature-title">Signature
